import { VisualBuilder } from "@/features/builder/visual-builder";

export default function NewThemeBuilderPage() {
  return (
    <main className="h-screen overflow-hidden bg-zinc-50">
      <VisualBuilder />
    </main>
  );
}
