import {
  getAssets,
  createAsset,
  updateAsset,
  deleteAsset,
} from "@/server/admin/actions/asset-actions";

export const assetService = {
  getAssets,
  createAsset,
  updateAsset,
  deleteAsset,
};
